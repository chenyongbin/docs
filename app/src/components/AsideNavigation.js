import React from "react";
import { Layout } from "../variables";

export default class AsideNavigation extends React.Component {
  render() {
    return (
      <div className={`${Layout.asideNavigation} docs-aside-navigation`}></div>
    );
  }
}
